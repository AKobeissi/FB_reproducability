# Domain-Adapted Financial RAG — Python dependencies
#
# Most of these are already installed in the existing venv.
# Install any missing ones with:
#   pip install -r domain_adapted_retrieval/requirements.txt

# Core retrieval & embeddings
sentence-transformers>=2.7.0
chromadb>=0.4.0

# PDF text extraction
pdfplumber>=0.10.0

# LLM inference (Qwen for HyDE and generation)
transformers>=4.40.0
accelerate>=0.28.0
bitsandbytes>=0.43.0          # 4-bit quantisation (GPU only)

# Evaluation metrics
sacrebleu>=2.3.0
rouge-score>=0.1.2

# Utilities
tqdm>=4.66.0
numpy>=1.24.0
matplotlib>=3.8.0
